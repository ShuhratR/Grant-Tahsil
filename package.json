{
  "scripts": {
    "start": "node server.js",
    "build": "npm run check",
    "check": "node --check app.js && node --check api/applications.js",
    "check:local": "npm run check && node --check server.js"
  }
}
